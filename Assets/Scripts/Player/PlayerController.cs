using UnityEngine;

namespace Player
{
    public class PlayerController : MonoBehaviour
    {
        private int _points = 0;
        private int _maxHeels = 100;
        private int _curentHeels = 50;
        private int _maxPower = 100;
        private int _curentPower = 50;
       
        // Start is called before the first frame update
       
         
        //     • getPower
        // • setPower
        // • setHeeles
        // • getHeeles
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
